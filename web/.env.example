# Where the Python engine is reachable. Local development points at a `run.py api`
# process; on Vercel this is set in Project Settings -> Environment Variables to the
# public URL of the deployed engine.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
