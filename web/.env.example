# Public URL where the Python tutoring engine is reachable.
NEXT_PUBLIC_API_URL=
# Server-only base URL for the managed Neon Auth instance.
NEON_AUTH_BASE_URL=
# Server-only signing secret for first-party auth cookies, at least 32 characters.
NEON_AUTH_COOKIE_SECRET=
