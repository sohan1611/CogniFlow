# CogniFlow - pinned to the resolved set verified on Python 3.14.6
langgraph==1.2.11
langgraph-checkpoint-sqlite==3.1.1
langchain-core==1.6.1
langchain-anthropic==1.7.0
langchain-google-genai==4.3.7
langchain-groq==1.1.3
chromadb==1.5.9
onnxruntime==1.29.0
pydantic==2.13.5
pydantic-settings==2.15.0
networkx==3.6.1
pypdf==6.16.2
python-dotenv==1.2.3
structlog==26.1.0
typer==0.27.2
streamlit==1.63.0
# The HTTP API. Both already arrive as chromadb transitive dependencies, but a
# service that depends on a transitive pin is one upstream release from breaking.
fastapi==0.141.1
uvicorn==0.52.4
# The hosted student store. Only used when DATABASE_URL is set; SQLite otherwise.
psycopg[binary]==3.2.10
psycopg-pool==3.3.1
