.venv/
__pycache__/
*.py[cod]
.env
*.db
*.sqlite
*.sqlite3
data/chroma/
data/cogniflow.db
.cache/llm_replay/
.pytest_cache/
results/
.agents/codex-*.txt
build/

# The demo video lives on the v1.0-stage1 GitHub Release, not in git history.
# It is 41 MB; committing it would roughly double the clone a judge has to make
# for a file they can stream instead.
*.mp4
*.mov
*.mkv
